
base 		  = ../datasets	# optional -- falls back to --base if omitted

Nx			  =  64
Ny			  =  64

min_step 	  = 3000       	# skip snapshots earlier than this step
min_stdev_phi = 0.01      	# skip snapshots whose stdev_phi is below this
min_std_deriv = 0.0002		# skip time derivatives whose stdev is below this

# force 	      = True

augment       = True		# False: speed things up for quick tests



# Stage 1a
#   autoencoder

latent_channels = 6
latent_spatial_size= 8   	# optional, defaults to 8 

stats_weight  = 0.01     	# weight of L_stats relative to L_recon

epochs 		  =  20			# note: stage 1 is augmented
patience 	  =   4       	# renamed to early_stopping_patience internally
batches		  = 128


# Stage 1b
#   derivative decoder

latent_channels = 6

stats_weight  = 0.5     	# weight of L_stats1 relative to L_recon1

epochs 		  =  50			# note: stage 1b is augmented
patience 	  =   5        	# renamed to early_stopping_patience internally
batches		  = 128

freeze_encoder= True
cos_weight	  = 2

# force 	      = True

# Training -- recon1: 371, stats1: 72 (0.01*stats1: 0.720)


# Stage 2
#   latent-space validation (interpolation-consistency fine-tuning)

recon1_weight = 0.0005		# multiplies a number 3 orders of magnitude larger
stats_weight  = 0.01     	# weight of L_stats0 relative to L_recon0
stats1_weight = 0.0002    	# weight of L_stats1 relative to L_recon0
deriv_weight  = 0.02
n_frozen_stages= 2

epochs 		  =  100		# note: stage 2 is not augmented
patience 	  =   10
batches 	  = same

force 	      = True

# Training -- recon1: 615 (0.001*recon1: 0.615), stats1: 1230 (0.001*stats1: 1.23)


# Stage 3a
#   latent dynamics surrogate (1step)
n_rollout_steps= 1
stats_weight  = 0.01

batches		  = 512
epochs 		  = 500
patience 	  =  50

force 	      = True


# Stage 3b
#   latent dynamics surrogate (rollout)
n_rollout_steps= 2
stats_weight  = 0.01
one_step_weight= 0.

batches		  = 512
epochs 		  = 500
patience 	  =  50

force 	      = True


# Stage 4
#   encoder refinement
rollout_weight = 1.0        # primary term for stage 4 (matches the default, but explicit is safer)
recon_weight   = 0.5       # small but NONZERO anchor -- this is a real design choice
stats_weight   = 0.01

n_rollout_steps= same

batches		  = 256
epochs 		  =  50
patience 	  =  10

force 	      = True


# Stage 5
#   end-to-end refinement
rollout_weight = 0.2        # now secondary -- see reasoning below
recon_weight   = 1.0        # now primary, per the original design intent
stats_weight   = 0.01

n_rollout_steps= same      # match stage 4's -- same reasoning as before

batches        = 256
epochs         =  30
patience       =   5