
base 		  = ../datasets	# optional -- falls back to --base if omitted

Nx			  =  64
Ny			  =  64

min_step 	  = 3000       	# skip snapshots earlier than this step
min_stdev_phi = 0.01      	# skip snapshots whose stdev_phi is below this
min_std_deriv = 0.15e-3		# skip time derivatives whose stdev is below this
min_passing_steps= 12		# any run with fewer than that many steps (out of 65) clearing `min_stdev_phi` is dropped entirely (all its steps, not just the sub-threshold ones)

recon0_scale  = 0.1e-3
stats0_scale  = 0.01

deriv_scale   = 0.02e-3
rollout_scale = 1e-6

# force 	      = True

augment       = True		# False: speed things up for quick tests
condition_on_theta= True




# Stage 1
#   autoencoder

latent_spatial_size= 8
latent_channels = 8  		# 8 * 8x8 = 512 DoF  [64x64 = 4096]

stats0_weight  = 1  # was 0.01     	# weight of L_stats relative to L_recon

epochs 		  =  60	 # change to 40?		# note: stage 1 is augmented
patience 	  =  10       	# renamed to early_stopping_patience internally
batches		  = 256

# force 	      = True



# Stage 2
#   latent-space structure: z1 approximates d z0 / dt

latent_channels = 8

stats0_weight   = 0.5     	# weight of L_stats0 relative to L_recon0
deriv_weight    = 4.

n_frozen_stages= 1
z0_from_deriv_weight=0.05	# in [0, 1]: 0 means z0 is trained on reconstruction alone, not `L_deriv`

epochs 		  = 100
patience 	  =  20
deriv_weight_warmup_epochs= 15  # with 10: training loss increases slightly at epochs 5-10

batches 	  = same

# force 	      = True



# Stage 3a
#   latent dynamics surrogate (1step)
n_rollout_steps= 1
ae_stats_weight= 1.  # was 0.01 (and was `stats0_weight`)

use_dt_decade_weights = True  # was (de facto) False

epochs 		  =1000
patience 	  =  50

num_workers	  =   8   # was 8
batches		  = 4096

lr 			  = 0.2e-3  # was 20e-6,  default: 0.001
z0_noise_scale=   0.175
dt_cap		  = 125   # 125/0.05 = 2500 time steps

# force 	      = True



# Stage 3b
#   latent dynamics surrogate (rollout)
n_rollout_steps= 2

ae_stats_weight= 1.  # was 0.01 (and was `stats0_weight`)
one_step_weight= 0.5

use_dt_decade_weights = True  # was (de facto) False

epochs 		  =1000
patience 	  =  50

num_workers	  = same
batches		  = same

lr 			  =   0.2e-3  # default: 0.001
z0_noise_scale=   0.1
dt_cap		  = same

force 	      = True



# Stage 4
#   encoder refinement
rollout_weight = 1.0        # primary term for stage 4 (matches the default, but explicit is safer)
recon0_weight   = 0.2  # was 0.5       # small but > 0 anchor
stats0_weight   = 1  # was 0.01

n_rollout_steps= same

batches		  = 128
epochs 		  =   0		# 50
patience 	  =   5 	# tends to shoot up: patience won't pay

force 	      = True



# Stage 5
#   end-to-end refinement
rollout_weight = 0.2        # now secondary -- see reasoning below
recon0_weight   = 1  # was 1.0        # now primary, per the original design intent
stats0_weight   = 1  # was 0.01

n_rollout_steps= same      # match stage 4's -- same reasoning as before

batches        = 128
epochs         =  0		# 30
patience       =   5

force 	       = True